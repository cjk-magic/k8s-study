apiVersion: apps/v1
kind: Deployment
metadata:
  name: rollout-demo
spec:
  replicas: 3
  selector:
  matchLabels:
    app: rollout-demo
  template:
    metadata:
      labels:
        app: rollout-demo
    spec:
      containers:
      - name: webserver
        image: nginx:1.17
        ports:
        - containerPort: 80